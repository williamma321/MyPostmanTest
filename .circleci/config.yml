version: 2.1
orbs:
  newman: postman/newman
jobs:
  newman-collection-run:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./STest.postman_collection.json
          environment: ./STest.postman_environment.json
          iteration-data: ./badUserpwd.json
          iteration-count: 4
          #reporters: cli,junit
